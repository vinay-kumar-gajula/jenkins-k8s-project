apiVersion: v1
kind: Service

metadata:
  name: node-exporter
  namespace: monitoring
  labels:
    app: node-exporter

  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/metrics"
    prometheus.io/port: "9100"

spec:
  type: ClusterIP
  clusterIP: None

  selector:
    app: node-exporter

  ports:
    - name: metrics
      protocol: TCP
      port: 9100
      targetPort: metrics
